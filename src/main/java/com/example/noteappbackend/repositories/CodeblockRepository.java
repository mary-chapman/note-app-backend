package com.example.noteappbackend.repositories;

import com.example.noteappbackend.models.Codeblock;
import org.springframework.data.repository.CrudRepository;

public interface CodeblockRepository extends CrudRepository<Codeblock, Long> {
}
