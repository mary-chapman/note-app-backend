package com.example.noteappbackend.repositories;

import com.example.noteappbackend.models.Para;
import org.springframework.data.repository.CrudRepository;

public interface ParaRepository extends CrudRepository<Para, Long> {
}
